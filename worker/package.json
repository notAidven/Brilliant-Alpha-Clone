{
  "name": "suited-ai-proxy",
  "version": "0.0.0",
  "description": "Free-tier Cloudflare Worker proxy for OpenAI, auth-gated by Firebase ID tokens (Suited poker app)",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "dry-run": "wrangler deploy --dry-run --outdir dist"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260625.1",
    "typescript": "^6.0.3",
    "vitest": "^3.2.4",
    "wrangler": "^4.105.0"
  }
}
